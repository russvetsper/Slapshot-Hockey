@if (@Model.Count == 0)
{

}
else
{
  <h1>Here is your list of teams:</h1>
  <ul>
    @foreach (var team in Model)
    {
      <li>@team.GetTeamName()</li>
    }
  </ul>
  <form action="/teams/delete" method="post">
    <button type="submit">Clear all teams</button>
  </form>
}
<p><a href="/teams/new">Add a new client</a></p>
<p><a href="/">Return home</a></p>
